 #using python command
  python -m venv <env_name>

  activate- <env_name>\scripts\activate

#using virtualenv
  virtualenv -p python3 <env_name>

#using conda
  conda create -p <env_name> puthon==<pythonversion> -y

#requirements
 pip install -r requirements.txt